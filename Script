--[[
    @kotyk_software HUB
    Rayfield UI + Universal Silent Aim + Key System with DEMO
    Author: @kotyk_software
    Key: @kotyk_software
    Ключ купить у: @kotyk_prostoy
]]

local Players = game:GetService("Players")
local LP = Players.LocalPlayer

--==========================================================
--          KEY SYSTEM + DEMO
--==========================================================
local VALID_KEY = "@kotyk_software"

local function keyCheck()
    local PG = LP:WaitForChild("PlayerGui")
    local kgui = Instance.new("ScreenGui")
    kgui.Name = "kotyk_key"
    kgui.ResetOnSpawn = false
    kgui.Parent = PG
    kgui.DisplayOrder = 999

    local bg = Instance.new("Frame")
    bg.Size = UDim2.new(0, 360, 0, 240)
    bg.Position = UDim2.new(0.5, -180, 0.5, -120)
    bg.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    bg.BorderSizePixel = 0
    bg.Parent = kgui
    Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 12)
    local stroke = Instance.new("UIStroke", bg)
    stroke.Color = Color3.fromRGB(255, 90, 90)
    stroke.Thickness = 1.5

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 42)
    title.BackgroundTransparency = 1
    title.Text = "@kotyk_software"
    title.TextColor3 = Color3.fromRGB(255, 90, 90)
    title.TextSize = 22
    title.Font = Enum.Font.GothamBold
    title.Parent = bg

    local sub = Instance.new("TextLabel")
    sub.Size = UDim2.new(1, 0, 0, 16)
    sub.Position = UDim2.new(0, 0, 0, 40)
    sub.BackgroundTransparency = 1
    sub.Text = "Введите ключ или попробуйте DEMO"
    sub.TextColor3 = Color3.fromRGB(150, 150, 160)
    sub.TextSize = 11
    sub.Font = Enum.Font.Gotham
    sub.Parent = bg

    local box = Instance.new("TextBox")
    box.Size = UDim2.new(1, -40, 0, 38)
    box.Position = UDim2.new(0, 20, 0, 66)
    box.BackgroundColor3 = Color3.fromRGB(28, 28, 34)
    box.BorderSizePixel = 0
    box.PlaceholderText = "Key..."
    box.Text = ""
    box.TextColor3 = Color3.fromRGB(230, 230, 235)
    box.PlaceholderColor3 = Color3.fromRGB(120, 120, 130)
    box.TextSize = 14
    box.Font = Enum.Font.GothamMedium
    box.ClearTextOnFocus = false
    box.Parent = bg
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 6)

    -- Submit
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.5, -25, 0, 38)
    btn.Position = UDim2.new(0, 20, 0, 112)
    btn.BackgroundColor3 = Color3.fromRGB(255, 90, 90)
    btn.Text = "Submit"
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamBold
    btn.Parent = bg
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)

    -- DEMO
    local demoBtn = Instance.new("TextButton")
    demoBtn.Size = UDim2.new(0.5, -25, 0, 38)
    demoBtn.Position = UDim2.new(0.5, 5, 0, 112)
    demoBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    demoBtn.Text = "DEMO"
    demoBtn.TextColor3 = Color3.fromRGB(230, 230, 235)
    demoBtn.TextSize = 14
    demoBtn.Font = Enum.Font.GothamBold
    demoBtn.Parent = bg
    Instance.new("UICorner", demoBtn).CornerRadius = UDim.new(0, 6)

    local status = Instance.new("TextLabel")
    status.Size = UDim2.new(1, -20, 0, 40)
    status.Position = UDim2.new(0, 10, 0, 158)
    status.BackgroundTransparency = 1
    status.Text = ""
    status.TextColor3 = Color3.fromRGB(255, 90, 90)
    status.TextSize = 12
    status.Font = Enum.Font.GothamMedium
    status.TextWrapped = true
    status.Parent = bg

    local footer = Instance.new("TextLabel")
    footer.Size = UDim2.new(1, 0, 0, 16)
    footer.Position = UDim2.new(0, 0, 1, -20)
    footer.BackgroundTransparency = 1
    footer.Text = "Купить ключ у @kotyk_prostoy"
    footer.TextColor3 = Color3.fromRGB(120, 120, 130)
    footer.TextSize = 11
    footer.Font = Enum.Font.Gotham
    footer.Parent = bg

    local done = false

    -- === DEMO (3 секунды, ничего не работает) ===
    demoBtn.MouseButton1Click:Connect(function()
        box.Text = ""
        status.TextColor3 = Color3.fromRGB(255, 200, 80)
        status.Text = "Это демо-режим.\nКупите ключ у @kotyk_prostoy"
        demoBtn.Text = "3..."
        task.wait(1); if demoBtn.Parent then demoBtn.Text = "2..." end
        task.wait(1); if demoBtn.Parent then demoBtn.Text = "1..." end
        task.wait(1)
        if kgui and kgui.Parent then kgui:Destroy() end
        warn("[@kotyk_software] DEMO MODE — купите ключ у @kotyk_prostoy")
    end)

    -- === Submit ===
    local function try()
        if box.Text == VALID_KEY then
            done = true
            status.Text = "Access granted!"
            status.TextColor3 = Color3.fromRGB(80, 200, 120)
            task.wait(0.4)
            if kgui and kgui.Parent then kgui:Destroy() end
        else
            status.Text = "Неверный ключ!\nКупите у @kotyk_prostoy"
            status.TextColor3 = Color3.fromRGB(255, 90, 90)
            box.Text = ""
        end
    end
    btn.MouseButton1Click:Connect(try)
    box.FocusLost:Connect(function(enter) if enter then try() end end)

    repeat task.wait(0.1) until done or not kgui.Parent
    return done
end

if not keyCheck() then
    return
end

--==========================================================
--          RAYFIELD UI
--==========================================================
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "@kotyk_software | Silent Aim",
    LoadingTitle = "@kotyk_software",
    LoadingSubtitle = "by Averiias & kotyk_software",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "kotyk_software",
        FileName = "SilentAimConfig"
    },
    Discord = {
        Enabled = false,
        Invite = "",
        RememberJoins = true
    },
    KeySystem = false -- уже проверено выше
})

--==========================================================
--          SETTINGS
--==========================================================
getgenv().SilentAimSettings = getgenv().SilentAimSettings or {
    Enabled = false,
    ToggleKey = "RightAlt",
    TeamCheck = false,
    VisibleCheck = false,
    TargetPart = "HumanoidRootPart",
    SilentAimMethod = "Raycast",
    FOVRadius = 130,
    FOVVisible = false,
    ShowSilentAimTarget = false,
    MouseHitPrediction = false,
    MouseHitPredictionAmount = 0.165,
    HitChance = 100
}

local Settings = getgenv().SilentAimSettings
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Mouse = LP:GetMouse()

local mouse_box = Drawing.new("Square")
mouse_box.Visible = false
mouse_box.ZIndex = 999
mouse_box.Color = Color3.fromRGB(54, 57, 241)
mouse_box.Thickness = 2
mouse_box.Size = Vector2.new(10, 10)
mouse_box.Filled = true

local fov_circle = Drawing.new("Circle")
fov_circle.Thickness = 1
fov_circle.NumSides = 100
fov_circle.Radius = Settings.FOVRadius
fov_circle.Filled = false
fov_circle.Visible = false
fov_circle.ZIndex = 999
fov_circle.Transparency = 1
fov_circle.Color = Color3.fromRGB(54, 57, 241)

--==========================================================
--          UTILS
--==========================================================
local function getPositionOnScreen(Vector)
    local Vec3, OnScreen = Camera:WorldToScreenPoint(Vector)
    return Vector2.new(Vec3.X, Vec3.Y), OnScreen
end

local function getDirection(Origin, Position)
    return (Position - Origin).Unit * 1000
end

local function getMousePosition()
    return UserInputService:GetMouseLocation()
end

local function IsPlayerVisible(Player)
    local PlayerCharacter = Player.Character
    local LocalPlayerCharacter = LP.Character
    if not (PlayerCharacter or LocalPlayerCharacter) then return end
    local PlayerRoot = PlayerCharacter:FindFirstChild(Settings.TargetPart) or PlayerCharacter:FindFirstChild("HumanoidRootPart")
    if not PlayerRoot then return end
    local CastPoints = {PlayerRoot.Position, LocalPlayerCharacter, PlayerCharacter}
    local IgnoreList = {LocalPlayerCharacter, PlayerCharacter}
    local ObscuringObjects = #Camera:GetPartsObscuringTarget(CastPoints, IgnoreList)
    return ObscuringObjects == 0
end

local function getClosestPlayer()
    local Closest, DistanceToMouse
    for _, Player in ipairs(Players:GetPlayers()) do
        if Player == LP then continue end
        if Settings.TeamCheck and Player.Team == LP.Team then continue end
        local Character = Player.Character
        if not Character then continue end
        if Settings.VisibleCheck and not IsPlayerVisible(Player) then continue end
        local HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")
        local Humanoid = Character:FindFirstChild("Humanoid")
        if not HumanoidRootPart or not Humanoid or Humanoid.Health <= 0 then continue end
        local ScreenPosition, OnScreen = getPositionOnScreen(HumanoidRootPart.Position)
        if not OnScreen then continue end
        local Distance = (getMousePosition() - ScreenPosition).Magnitude
        if Distance <= (DistanceToMouse or Settings.FOVRadius) then
            Closest = HumanoidRootPart
            DistanceToMouse = Distance
        end
    end
    return Closest
end

local function CalculateChance(Percentage)
    Percentage = math.floor(Percentage)
    local rng = Random.new()
    local chance = math.floor(rng:NextNumber(0, 1) * 100) / 100
    return chance <= Percentage / 100
end

--==========================================================
--          TABS
--==========================================================
local MainTab    = Window:CreateTab("Main", 4483362458)
local VisualsTab = Window:CreateTab("Visuals", 4483362458)
local ConfigTab  = Window:CreateTab("Config", 4483362458)

--==========================================================
--          MAIN TAB
--==========================================================
MainTab:CreateSection("Silent Aim")

MainTab:CreateToggle({
    Name = "Enable Silent Aim",
    CurrentValue = false,
    Flag = "SilentAimEnabled",
    Callback = function(Value)
        Settings.Enabled = Value
    end
})

MainTab:CreateKeybind({
    Name = "Toggle Key",
    CurrentKeybind = "RightAlt",
    HoldToInteract = false,
    Flag = "SilentAimKey",
    Callback = function(Keybind)
        Settings.ToggleKey = Keybind
    end
})

MainTab:CreateToggle({
    Name = "Team Check",
    CurrentValue = false,
    Flag = "TeamCheck",
    Callback = function(Value)
        Settings.TeamCheck = Value
    end
})

MainTab:CreateToggle({
    Name = "Visible Check",
    CurrentValue = false,
    Flag = "VisibleCheck",
    Callback = function(Value)
        Settings.VisibleCheck = Value
    end
})

MainTab:CreateDropdown({
    Name = "Target Part",
    Options = {"Head", "HumanoidRootPart", "Random"},
    CurrentOption = {"HumanoidRootPart"},
    Flag = "TargetPart",
    Callback = function(Option)
        if typeof(Option) == "table" then Option = Option[1] end
        Settings.TargetPart = Option
    end
})

MainTab:CreateDropdown({
    Name = "Silent Aim Method",
    Options = {"Raycast", "FindPartOnRay", "FindPartOnRayWithWhitelist", "FindPartOnRayWithIgnoreList", "Mouse.Hit/Target"},
    CurrentOption = {"Raycast"},
    Flag = "SilentAimMethod",
    Callback = function(Option)
        if typeof(Option) == "table" then Option = Option[1] end
        Settings.SilentAimMethod = Option
    end
})

MainTab:CreateSlider({
    Name = "Hit Chance",
    Range = {0, 100},
    Increment = 1,
    Suffix = "%",
    CurrentValue = 100,
    Flag = "HitChance",
    Callback = function(Value)
        Settings.HitChance = Value
    end
})

--==========================================================
--          VISUALS TAB
--==========================================================
VisualsTab:CreateSection("Field of View")

VisualsTab:CreateToggle({
    Name = "Show FOV Circle",
    CurrentValue = false,
    Flag = "FOVVisible",
    Callback = function(Value)
        Settings.FOVVisible = Value
        fov_circle.Visible = Value
    end
})

VisualsTab:CreateSlider({
    Name = "FOV Circle Radius",
    Range = {0, 360},
    Increment = 1,
    Suffix = "px",
    CurrentValue = 130,
    Flag = "FOVRadius",
    Callback = function(Value)
        Settings.FOVRadius = Value
        fov_circle.Radius = Value
    end
})

VisualsTab:CreateColorPicker({
    Name = "FOV Circle Color",
    Color = Color3.fromRGB(54, 57, 241),
    Flag = "FOVColor",
    Callback = function(Value)
        fov_circle.Color = Value
    end
})

VisualsTab:CreateToggle({
    Name = "Show Silent Aim Target",
    CurrentValue = false,
    Flag = "ShowTarget",
    Callback = function(Value)
        Settings.ShowSilentAimTarget = Value
    end
})

VisualsTab:CreateColorPicker({
    Name = "Target Box Color",
    Color = Color3.fromRGB(54, 57, 241),
    Flag = "TargetColor",
    Callback = function(Value)
        mouse_box.Color = Value
    end
})

VisualsTab:CreateSection("Prediction")

VisualsTab:CreateToggle({
    Name = "Mouse.Hit/Target Prediction",
    CurrentValue = false,
    Flag = "Prediction",
    Callback = function(Value)
        Settings.MouseHitPrediction = Value
    end
})

VisualsTab:CreateSlider({
    Name = "Prediction Amount",
    Range = {0.165, 1},
    Increment = 0.001,
    Suffix = "s",
    CurrentValue = 0.165,
    Flag = "PredictionAmount",
    Callback = function(Value)
        Settings.MouseHitPredictionAmount = Value
    end
})

--==========================================================
--          CONFIG TAB
--==========================================================
ConfigTab:CreateSection("Configuration")

ConfigTab:CreateButton({
    Name = "Save Settings",
    Callback = function()
        Rayfield:SaveConfiguration()
        Rayfield:Notify({
            Title = "@kotyk_software",
            Content = "Настройки сохранены!",
            Duration = 3
        })
    end
})

ConfigTab:CreateButton({
    Name = "Load Settings",
    Callback = function()
        Rayfield:LoadConfiguration()
        local cfg = Rayfield:GetConfiguration()
        if cfg then
            Settings.Enabled = cfg.SilentAimEnabled or false
            Settings.TeamCheck = cfg.TeamCheck or false
            Settings.VisibleCheck = cfg.VisibleCheck or false
            Settings.TargetPart = cfg.TargetPart or "HumanoidRootPart"
            Settings.SilentAimMethod = cfg.SilentAimMethod or "Raycast"
            Settings.FOVRadius = cfg.FOVRadius or 130
            Settings.FOVVisible = cfg.FOVVisible or false
            Settings.ShowSilentAimTarget = cfg.ShowTarget or false
            Settings.MouseHitPrediction = cfg.Prediction or false
            Settings.MouseHitPredictionAmount = cfg.PredictionAmount or 0.165
            Settings.HitChance = cfg.HitChance or 100
            fov_circle.Radius = Settings.FOVRadius
            fov_circle.Visible = Settings.FOVVisible
        end
        Rayfield:Notify({
            Title = "@kotyk_software",
            Content = "Настройки загружены!",
            Duration = 3
        })
    end
})

ConfigTab:CreateButton({
    Name = "Reset to Default",
    Callback = function()
        Settings.Enabled = false
        Settings.TeamCheck = false
        Settings.VisibleCheck = false
        Settings.TargetPart = "HumanoidRootPart"
        Settings.SilentAimMethod = "Raycast"
        Settings.FOVRadius = 130
        Settings.FOVVisible = false
        Settings.ShowSilentAimTarget = false
        Settings.MouseHitPrediction = false
        Settings.MouseHitPredictionAmount = 0.165
        Settings.HitChance = 100
        fov_circle.Radius = 130
        fov_circle.Visible = false
        Rayfield:Notify({
            Title = "@kotyk_software",
            Content = "Настройки сброшены!",
            Duration = 3
        })
    end
})

--==========================================================
--          SILENT AIM HOOKS
--==========================================================
local ExpectedArguments = {
    FindPartOnRayWithIgnoreList = {ArgCountRequired = 3, Args = {"Instance", "Ray", "table", "boolean", "boolean"}},
    FindPartOnRayWithWhitelist = {ArgCountRequired = 3, Args = {"Instance", "Ray", "table", "boolean"}},
    FindPartOnRay = {ArgCountRequired = 2, Args = {"Instance", "Ray", "Instance", "boolean", "boolean"}},
    Raycast = {ArgCountRequired = 3, Args = {"Instance", "Vector3", "Vector3", "RaycastParams"}}
}

local function ValidateArguments(Args, RayMethod)
    local Matches = 0
    if #Args < RayMethod.ArgCountRequired then return false end
    for Pos, Argument in pairs(Args) do
        if typeof(Argument) == RayMethod.Args[Pos] then
            Matches = Matches + 1
        end
    end
    return Matches >= RayMethod.ArgCountRequired
end

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(...)
    local Method = getnamecallmethod()
    local Arguments = {...}
    local self = Arguments[1]
    local chance = CalculateChance(Settings.HitChance)

    if Settings.Enabled and self == workspace and not checkcaller() and chance then
        if Method == "FindPartOnRayWithIgnoreList" and Settings.SilentAimMethod == Method then
            if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRayWithIgnoreList) then
                local A_Ray = Arguments[2]
                local HitPart = getClosestPlayer()
                if HitPart then
                    local Origin = A_Ray.Origin
                    Arguments[2] = Ray.new(Origin, getDirection(Origin, HitPart.Position))
                    return oldNamecall(unpack(Arguments))
                end
            end
        elseif Method == "FindPartOnRayWithWhitelist" and Settings.SilentAimMethod == Method then
            if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRayWithWhitelist) then
                local A_Ray = Arguments[2]
                local HitPart = getClosestPlayer()
                if HitPart then
                    local Origin = A_Ray.Origin
                    Arguments[2] = Ray.new(Origin, getDirection(Origin, HitPart.Position))
                    return oldNamecall(unpack(Arguments))
                end
            end
        elseif (Method == "FindPartOnRay" or Method == "findPartOnRay") and Settings.SilentAimMethod == Method then
            if ValidateArguments(Arguments, ExpectedArguments.FindPartOnRay) then
                local A_Ray = Arguments[2]
                local HitPart = getClosestPlayer()
                if HitPart then
                    local Origin = A_Ray.Origin
                    Arguments[2] = Ray.new(Origin, getDirection(Origin, HitPart.Position))
                    return oldNamecall(unpack(Arguments))
                end
            end
        elseif Method == "Raycast" and Settings.SilentAimMethod == Method then
            if ValidateArguments(Arguments, ExpectedArguments.Raycast) then
                local A_Origin = Arguments[2]
                local HitPart = getClosestPlayer()
                if HitPart then
                    Arguments[3] = getDirection(A_Origin, HitPart.Position)
                    return oldNamecall(unpack(Arguments))
                end
            end
        end
    end
    return oldNamecall(...)
end))

local oldIndex
oldIndex = hookmetamethod(game, "__index", newcclosure(function(self, Index)
    if self == Mouse and not checkcaller() and Settings.Enabled and Settings.SilentAimMethod == "Mouse.Hit/Target" then
        local HitPart = getClosestPlayer()
        if HitPart then
            if Index == "Target" or Index == "target" then
                return HitPart
            elseif Index == "Hit" or Index == "hit" then
                return (Settings.MouseHitPrediction and (HitPart.CFrame + (HitPart.Velocity * Settings.MouseHitPredictionAmount))) or (not Settings.MouseHitPrediction and HitPart.CFrame)
            end
        end
    end
    return oldIndex(self, Index)
end))

--==========================================================
--          RENDER LOOP
--==========================================================
RunService.RenderStepped:Connect(function()
    if Settings.ShowSilentAimTarget and Settings.Enabled then
        local Target = getClosestPlayer()
        if Target then
            local Pos, OnScreen = Camera:WorldToViewportPoint(Target.Position)
            mouse_box.Visible = OnScreen
            mouse_box.Position = Vector2.new(Pos.X, Pos.Y)
        else
            mouse_box.Visible = false
        end
    else
        mouse_box.Visible = false
    end

    if Settings.FOVVisible then
        fov_circle.Visible = true
        fov_circle.Position = getMousePosition()
    else
        fov_circle.Visible = false
    end
end)

--==========================================================
Rayfield:Notify({
    Title = "@kotyk_software",
    Content = "Silent Aim успешно загружен!",
    Duration = 5
})

print("[@kotyk_software] Key accepted. Rayfield UI + Universal Silent Aim loaded.")